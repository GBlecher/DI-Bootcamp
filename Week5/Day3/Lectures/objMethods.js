let myObj = {
    name : "John",
    lastName : "Doe",
    age : 25,
    friends : ["Mark", "Lucie", "Ana"]
}

objKeyArr = Object.keys(myObj)
objKeyLength = objKeyArr.length
console.log(objLength + "keys")

objValArr = Object.values(myObj)
objValLength = objValArr.length

// Use the methods above to :

// Count how many keys and values are in the object below
// Display : "The x# key is : --- The x# value is : ---".