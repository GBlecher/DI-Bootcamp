import { INCREMENT, DECREMENT } from "./actions";


const initialState = {
    count:0
}

export const counterReducer = (state = initialState, action) => {
    return state
    // recomended to use switch case rather than if else
}