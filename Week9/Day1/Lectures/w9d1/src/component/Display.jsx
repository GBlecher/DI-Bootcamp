import ShowCounter from './ShowCounter';

const Display = (props) => {
    return (
        <div>
            
            <ShowCounter count={count} />
        </div>
    );
};

export default Display;